# Optional: where the SQLite file lives. Defaults to ./data/utm-tracker.db
UTM_DB_PATH=./data/utm-tracker.db

# Optional: the public base URL used when copying short-links from the dashboard.
# Defaults to http://localhost:3000 in dev.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
